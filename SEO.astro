---
// ── Configurar al iniciar un nuevo proyecto ──────────────────────────────────
const SITE = {
  name: "Base Astro",
  locale: "es_AR",
  defaultOgImage: `${import.meta.env.BASE_URL}images/og-image-1200x630.svg`,
  logo: `${import.meta.env.BASE_URL}images/logo.svg`,
  socials: [] as string[], // agregar URLs de redes sociales
} as const;

// Cambiar a true durante desarrollo/staging para evitar indexación prematura.
// En producción debe ser false — controlar noindex por página via prop noindex.
const NOINDEX_GLOBAL = false;

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogImageAlt?: string;
  canonicalURL?: string;
  ogType?: "website" | "article";
  noindex?: boolean;
}

const {
  title,
  description,
  ogImage,
  ogImageAlt,
  canonicalURL = Astro.url.href,
  ogType = "website",
  noindex = false,
} = Astro.props;

const base = Astro.site ?? Astro.url;
const siteUrl = new URL(import.meta.env.BASE_URL, base).href;
const ogImageUrl = new URL(ogImage ?? SITE.defaultOgImage, base).href;
const logoUrl = new URL(SITE.logo, base).href;

const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: SITE.name,
    url: siteUrl,
    logo: logoUrl,
    sameAs: SITE.socials,
  },
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    url: siteUrl,
    name: SITE.name,
  },
];
---

<title>{title}</title>
<meta name="description" content={description} />
<meta
  name="robots"
  content={NOINDEX_GLOBAL || noindex ? "noindex,nofollow" : "index,follow"}
/>
<link rel="canonical" href={canonicalURL} />

<!-- Open Graph -->
<meta property="og:site_name" content={SITE.name} />
<meta property="og:locale" content={SITE.locale} />
<meta property="og:type" content={ogType} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={ogImageUrl} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:image:alt" content={ogImageAlt ?? title} />

<!-- Twitter / X -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImageUrl} />

<!-- Structured Data -->
{
  jsonLd.map((schema) => (
    <script
      is:inline
      type="application/ld+json"
      set:html={JSON.stringify(schema)}
    />
  ))
}
